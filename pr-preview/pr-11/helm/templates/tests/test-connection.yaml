apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ibtisam-iq.fullname" . }}-test-connection"
  labels:
    {{- include "ibtisam-iq.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ibtisam-iq.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
