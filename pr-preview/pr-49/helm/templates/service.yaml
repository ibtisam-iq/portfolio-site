# The ClusterIP service in front of the pods. Ingress and HTTPRoute both target it.

apiVersion: v1
kind: Service
metadata:
  name: {{ include "ibtisam-iq.fullname" . }}
  labels:
    {{- include "ibtisam-iq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ibtisam-iq.selectorLabels" . | nindent 4 }}
